# Снимки прогонного стенда в репозиторий не кладём: это мегабайты
# картинок, которые перезаписываются на каждом прогоне.
tests/shots/

# Playwright для браузерных прогонов ставится в окружении, а не в репозиторий:
# зависимостей у самой игры нет и быть не должно.
node_modules/
package-lock.json
